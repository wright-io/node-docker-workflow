machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install docker-compose==1.2.0

test:
    override:
      - docker-compose run -d --no-deps web
      - cd app; mocha

deployment:
    hub:
      branch: master
      commands:
        - $DEPLOY